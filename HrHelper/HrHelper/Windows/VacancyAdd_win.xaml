<Window x:Class="HrHelper.Windows.VacancyAdd_win"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HrHelper.Windows"
        mc:Ignorable="d"
        Title="VacancyAdd_win" Height="250" Width="400" WindowStyle="None" Background="Transparent" WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" Icon="/Source/LogoMini.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border 
            Grid.RowSpan="3" Grid.ColumnSpan="2" Background="{StaticResource Channels}" CornerRadius="15"/>
        <Border 
            Grid.Row="0"  Grid.ColumnSpan="2" Background="{StaticResource Servers}" CornerRadius="15,15,0,0">
            <Border.OpacityMask>
                <VisualBrush>
                    <VisualBrush.Visual>
                        <Border
                    Background="Black"
                    CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource FindAncestor, AncestorType=Border}}"
                    Width="{Binding ActualWidth, RelativeSource={RelativeSource FindAncestor, AncestorType=Border}}"
                    Height="{Binding ActualHeight, RelativeSource={RelativeSource FindAncestor, AncestorType=Border}}"
                    />
                    </VisualBrush.Visual>
                </VisualBrush>
            </Border.OpacityMask>
            <Grid x:Name="toolBar_grid" Grid.ColumnSpan="2" Background="Transparent" >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="minWind_but" Style="{StaticResource minWinBut}" Width="50" >
                        <Line X1="0" Y1="0" X2="70" Y2="0"  StrokeThickness="10" Margin="15,0">
                            <Line.Style>
                                <Style TargetType="Line">
                                    <Setter Property="Stroke" Value="{StaticResource Chat}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver,ElementName=minWind_but}" Value="true">
                                            <Setter Property="Stroke" Value="{StaticResource Channels}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Line.Style>
                        </Line>
                    </Button>
                    <Button x:Name="close_but" Style="{StaticResource closeBut}" >
                        <Path Data="M1,1L15,15M15,1L1,15" StrokeThickness="3.5">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Stroke" Value="{StaticResource Chat}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver,ElementName=close_but }" Value="true">
                                            <Setter Property="Stroke" Value="{StaticResource Channels}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                    </Button>

                </StackPanel>
            </Grid>
        </Border>



        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock x:Name="titile_tb" Text="Название вакансии" 
                       FontSize="17" Foreground="{StaticResource White}"
                       FontWeight="Bold"
                   Margin="10,10,10,0" TextAlignment="Left"
            />
            <TextBox x:Name="jobTitle_tb" Style="{StaticResource inputTextbox}" Width="370"/>
        </StackPanel>

        <Grid Grid.Row="2" Grid.ColumnSpan="2">
            <StackPanel 
                Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Добавить" x:Name="vacancyAdd_but"
                    Style="{StaticResource defaultBut}" Height="50" Click="vacancyAdd_but_Click"  />
            </StackPanel>
        </Grid>
    </Grid>
</Window>

